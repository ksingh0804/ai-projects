# Personal Progress — Steady

Complete a practice round to start live tracking.

Progress on the public website stays private in your browser (`localStorage`).  
When you run Steady locally with `python3 serve.py`, check-ins can also rewrite this file.
