{
  "dataset": "order_items",
  "owner": "oms-team",
  "description": "Line items within an order.",
  "primary_key": ["order_id", "product_id"],
  "freshness_sla_hours": 24,
  "columns": [
    {"name": "order_id", "type": "VARCHAR", "required": true},
    {"name": "product_id", "type": "VARCHAR", "required": true},
    {"name": "quantity", "type": "DOUBLE", "required": true, "min": 0},
    {"name": "unit_price", "type": "DOUBLE", "required": true, "min": 0},
    {"name": "line_total", "type": "DOUBLE", "required": true, "min": 0}
  ]
}
